# WATCHLIST — names that don't clear the gates YET, with re-evaluation triggers

## DKNG — bullish (call) idea, currently FAILS conviction  [added 2026-07-01]
- Idea: DKNG Aug 21 $27.5/$30 CALL DEBIT SPREAD (~$75 debit, $175 max, BE $28.25, 2.3:1).
- Why NOT now: fails the strength/conviction gate — DKNG $26.07 is BELOW its 200-DMA
  (-10.5%, ~$29.12), DEATH cross (50<200), not at a breakout (-47% from 52wk high), RSI 51.
  It's a mean-reversion bounce, not the momentum the call scan targets.
- Structure note: it's a MULTI-LEG spread — our MCP is single-leg only (+ account is Level 2),
  so it can only be placed MANUALLY in the Robinhood app, not by the agent.
- Earnings: TIME-STOP ok (ER 8/5, exit ~8/3).
- RE-EVALUATE IF EITHER TRIGGER HITS (then it earns the momentum setup):
  1. DKNG reclaims its 200-DMA (~$29.12), flipping toward a golden-cross setup, OR
  2. DKNG clears $30 (the 20-day high ~$30.13 = confirmed breakout).
- On a trigger: re-run the strength read + re-price the spread; if it clears, it's a manual
  bullish add that diversifies alongside KDP.
